{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "build:wasm": "wasm-pack build ../crates/wasm --target web --release --out-dir ../../web/src/wasm && rm -f src/wasm/.gitignore && mkdir -p public/wasm && cp src/wasm/wcsim_wasm_bg.wasm public/wasm/"
  },
  "dependencies": {
    "flag-icons": "^7.5.0",
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "puppeteer-core": "^25.1.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
